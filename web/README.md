# web

The Next.js dashboard + REST API for Invoice Intelligence.

See the [project README](../README.md) for what this does, architecture, setup,
deploy, and the REST API — this file used to be unmodified `create-next-app`
boilerplate and didn't describe the actual project.

```bash
npm install
npm run dev
```
